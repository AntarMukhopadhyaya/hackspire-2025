# SMTP and NextAuth are hardcoded in code as per request.
# These envs are no longer used.

# GMAIL_USER, GMAIL_APP_PASSWORD, CONTACT_EMAIL, NEXTAUTH_SECRET, NEXTAUTH_URL
# are embedded in `src/app/api/contact/route.ts`.
